# Backend Dependencies - Slim Build (v5.13)

# Core Web Framework
flask==3.0.3
flask-cors==4.0.0
gunicorn==21.2.0

# HTTP & Environment
requests==2.31.0
python-dotenv==1.0.0
urllib3<2.0

# Data Processing
numpy==1.26.4
# pandas is a dependency of yfinance, so pip will install it automatically, 
# but we can list it if we want to pin version. Let's let yfinance decide.

# Market Data

# AI Firm / RAG Dependencies
chromadb==0.4.15
pydantic==2.4.2

