# Backend Dependencies - Slim Build (v5.13)

# Core Web Framework
flask==3.0.3
flask-cors==6.0.0
gunicorn==22.0.0

# HTTP & Environment
requests==2.32.5
python-dotenv==1.0.0
urllib3==2.6.0
filelock==3.20.1
werkzeug==3.1.4

# Data Processing
numpy==1.26.4
# pandas is a dependency of yfinance, so pip will install it automatically,
# but we can list it if we want to pin version. Let's let yfinance decide.

# Market Data
# Add yfinance to provide fallback market data retrieval for symbols and historicals
yfinance==0.2.27

# AI Firm / RAG Dependencies
chromadb==0.4.15
pydantic==2.4.2
pysqlite3-binary==0.5.2.post3
typing_extensions>=4.5.0

# Database
sqlalchemy==2.0.23

